{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "node",
            "request": "launch",
            "program": "cpp/build/${command:cmake.launchTargetFilename}"
        }
    ]
}